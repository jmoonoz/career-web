import React from 'react'
import SideMenu from './SideMenu'

function Jobs() {
    return (
        <section id="Jobs">
            <h1>this is the job section</h1>
            <SideMenu />

        </section>

    )
}

export default Jobs
